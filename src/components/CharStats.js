const CharStats = [ 0, 0, 0, 0];

// Declaring a Character Stats array, which can be modified
// Imported into the other files 
// Use for displaying the user stats and the user radar chart 
// The values in the array are responsible for user statistics for workout, health, study and productivity respectively

export default CharStats